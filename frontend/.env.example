# Optional when the frontend and backend are deployed separately.
# Local development uses the proxy in vite.config.js; a unified production build
# uses same-origin API requests and does not need this variable.
VITE_API_BASE_URL=http://localhost:5050
